{
  "name": "Tailscale ExitNode",
  "capAdd": [
    "NET_ADMIN",
    "NET_RAW",
    "SYS_MODULE"
  ],
  "features": {
    "ghcr.io/tailscale/codespace/tailscale": {
      "version": "latest"
    }
  },
  "postStartCommand": "sudo tailscale up --authkey=${TAILSCALE_AUTHKEY} --advertise-exit-node --reset --accept-dns=false",
  "remoteUser": "codespace"
}
